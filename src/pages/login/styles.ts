import { IStyles } from '@/types';

export const styles = {
  container: {
    display: 'flex',
    justifyContent: 'center',
    height: '100vh',
    alignItems: 'center',
    backgroundColor: '#f5f5f5',
  },
} satisfies IStyles;
