import { IStyles } from '@/types';

export const styles = {
  container: {
    border: '1px solid #f1f3f5',
    borderRadius: '6px',
    minHeight: '40px',
    p: 2,

    display: 'flex',
    justifyContent: 'space-between',
  },
} satisfies IStyles;
